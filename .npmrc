# Electron requires postinstall to download platform-specific binaries
# pnpm caches aggressively and can skip postinstall - this prevents that
side-effects-cache=true

# Don't skip optional dependencies (native modules like better-sqlite3)
optional=true
